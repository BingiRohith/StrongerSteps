import { Users } from 'lucide-react';
import PagePlaceholder from '@/components/admin/PagePlaceholder';

export default function AdminTeamPage() {
  return (
    <PagePlaceholder
      icon={Users}
      title="Team"
      description="Manage the doctors and wellness experts shown on the About page once this section is built."
    />
  );
}
